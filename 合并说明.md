# 代码合并和开发说明

第1阶段：并入不冲突代码（增加但并不修改的）
第2阶段：融合冲突之处
第3阶段：分析并修改代码使其通过编译并无逻辑漏洞
第4阶段：优化和完善

# 可直接搜索 Jeffrey070318  查看修改或添加的内容

@todo 代码测试和整车的cmd一类的冲突，待完善
@todo 双车代码的处理问题   已完成
@todo 发现task没有处理    已解决
@todo cmd的处理问题，及使用单独的模块测试和总上位机共通的问题？   此部分还是等待阴学长的代码吧
@todo  剩下了cmd的双车兼容调整，以及代码无用的删除和精简
@todo   实测

@attention  阴学长貌似为了方便测试把定位代码的imu，光流计文件全放在app里，已移回，后续看修改情况   6.18  反正模块用的不多无所谓了，又移回去了
@attention  将general_define里面的枚举宏定义等移去robot_define
@attention  学长的框架用的是Makefile，小登的都是cmake，所以部分文件直接改了，学长在移植or查看时只需直接复制bsp，module，app三层即可.（如果cubemx配置没有被动的话）不过好像cmake和Makefile本就可以共通？
@attention 所有app层调参相关参数全部移去robot_define
@attention  已提供双车双参编译模式  

## 6.19 todo

@todo 机械臂动作链路需要优先整理：目前cmd会发布delta_action，但delta状态机还没有真正根据DELTA_STOP_ACT / DELTA_HIT / DELTA_READY等命令切换动作。
@todo Delta状态机需要补全接球/击球流程：当前DELTA_ORIGINAL_POS、GET_BALL、DELTA_SERVE_HIT_1、DELTA_SERVE_BACK_1等状态里还有较多测试逻辑和被注释的跳转，R1/R2两套参数已隔离，但动作流程还要实车确认。
@todo R1的Serve封装需要和Delta/CMD重新闭合：serve.c目前更多是本地状态机自跑，serve_cmd_recv没有真正参与状态切换，后续需要明确由Delta还是CMD触发发球杆动作。
@todo R2接球机械臂需要单独定义动作语义：当前R2暂用右开关非上位触发DELTA_HIT，后续应改成接球动作入口，并和视觉/上位机目标状态联动。
@todo 视觉自动流程还未形成完整整车状态机：现在只有target_x/target_y点到点导航，target_yaw、mode/state、到点后触发机械臂接球/发球等流程还没接上。
@todo 光流计接收仍需修正：当前optical_flow.c回调里用recv_buff_size解析，后续应改为实际recv_len，避免IDLE半包或残留buffer影响帧同步。
@todo 底盘安全和反馈还需补：功率限制、真实速度估计、消息超时保护、遥控器离线急停还未完成。
@todo 遥控器开关中位逻辑需要检查：当前部分模式只处理上/下位，中位可能沿用上一次速度或模式，实车前要避免危险状态残留。

